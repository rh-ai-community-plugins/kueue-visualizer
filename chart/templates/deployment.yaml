apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kueue-plugin.fullname" . }}
  labels:
    {{- include "kueue-plugin.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kueue-plugin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kueue-plugin.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "kueue-plugin.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: {{ include "kueue-plugin.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
